version: '3.8'

services:
  server:
    build: ./server
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env
    environment:
      - PORT=5000

  frontend:
    build: .
    restart: always
    ports:
      - "8080:80"
    depends_on:
      - server
